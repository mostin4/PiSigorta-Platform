{
  "name": "pisigorta-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy.js --network pichain"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "ethers": "^5.7.2",
    "hardhat": "^2.12.6"
  }
}
